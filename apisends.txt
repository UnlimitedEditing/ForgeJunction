[2026/03/16 12:54] Flux Don: trying to make my solution work with telegram inputs so a person could grab a prompt as is here and it get's parsed into a valid form to be passed to graydient. 

I have a node graph for chaining renders together, double clicking pops open a node with whatever is in your clipboard for quick setup (and because it would be nice for users to just grab and use materials from telegram and alter the prompt if they need to). 
I want to give them a dynamic form so they can select portions of the prompt and reuse the chain setup with a quick form to replace the sections of the prompts they want to manipulate. 
The idea is you open a chain of workflows and you can just type > enter > type > enter and it will replace each of the sections of the prompt configured to the chain so you can quickly redeploy a batch of renders with a new character or style adjustment etc. 

But parsing the /render with the <model> is... confusing me 😅 what does the api form look like for something like /render /size:whatever /bla:bla-bla <hentai-cinematic3-xl>? Because I can't seem to find a workflow associated with <hentai-cinematic3-xl> 🤦‍♂️ I barely ever use SD based stuff when I've made myself familiar with pd bot so I'm just maybe out of my depth
[2026/03/16 12:59] Russ: That's one of the interesting things about the API, the prompt field basically uses the exact same command format as telegram, so if you generate a render with the bot (regardless of /workflow vs /render) and copy that command and use it in the API prompt field, that will work exactly the same 

So the API form of /render /size:... /blah:... Is precisely that 🙂

There are "concepts" which are your full/checkpoint models and LoRAs, but there are also workflows

<hentai-cinematic3-xl> sounds like a full model concept, so you'd use it with /render rather than /workflow

Like /render /size:768x1024 A woman sitting at a diner table <hentai-cinematic3-xl> in both the PD bot and the API should both run using that model
[2026/03/16 13:01] Flux Don: AHA! It's me, I'm the problem again.... I created an unnecessary layer of abstraction because I just didn't get it 🫠 haha okay let me go remove a lot of work...