# API URL (usually no need to change this)
VITE_GRAYDIENT_API_URL=https://app.graydient.ai/api/v3/

# API key is now managed securely through the app's UI
# Launch the app and enter your key on the welcome screen
